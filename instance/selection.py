from instance.solution import Solutions
import numpy as np
from numba import njit


class Selection:
    def __init__(self, selection_method: str) -> None:
        self.selection_method = selection_method

    def select(self, solutions_array: np.ndarray) -> np.ndarray:
        instances = None
        return instances

    def _tournament():
        instances = None
        return instances